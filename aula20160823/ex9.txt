#include <stdio.h>
#include <string.h>

/*int main()
{
    char frase[50];
    printf("Insira uma frase:\n");
    fflush(stdin);
    gets(frase);
    printf("A frase tem %d caracteres\n\n",strlen(frase));
}*/ //Não tinha lido que não era pra usar comandos da string.h =/

int main()
{
    printf("\t\t\t\tQuantas letras\n\n");
    char frase[50];
    int caracteres=0;

    printf("Insira uma frase:\n");
    fflush(stdin);
    gets(frase);

    while (frase[caracteres] != '\0')
    {
        caracteres++;
    }
    printf("A frase tem %d caracteres\n\n",caracteres);


}